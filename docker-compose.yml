services:
  logger:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ./logs:/app/logs
      - ./keys:/app/keys:ro
      - ${HOME}/.pm2/pub.sock:/pm2.sock:ro
    environment:
      - LOGS_DIR=/app/logs
      - PORT=8080
      - PM2_SOCK=/pm2.sock
    restart: unless-stopped
